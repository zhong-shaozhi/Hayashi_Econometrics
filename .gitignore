# macOS system files — never commit these
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# R generated files — not source code
.Rhistory
.RData
.Rproj.user/
*.Rproj.user


# Raw data if downloaded as files (we use hayashir package, so less relevant)
data/raw/
